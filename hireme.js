document.addEventListener("DOMContentLoaded", function() {
    const container = document.getElementById('animation-container2');
    
    // Load the Lottie animation
    lottie.loadAnimation({
        container: container, // Corrected container ID
        renderer: 'svg', // Render as SVG
        loop: true, // Loop the animation
        autoplay: true, // Start playing automatically
        path: 'animation3.json' // Path to your animation JSON file
    });
    
    // Add the bounce class to create a bouncy effect
});

document.addEventListener("DOMContentLoaded", function() {
    const container = document.getElementById('animation-container');
    
    // Load the Lottie animation
    lottie.loadAnimation({
        container: container, // Corrected container ID
        renderer: 'svg', // Render as SVG
        loop: true, // Loop the animation
        autoplay: true, // Start playing automatically
        path: 'animation4.json' // Path to your animation JSON file
    });
    
    // Add the bounce class to create a bouncy effect
});

document.addEventListener("DOMContentLoaded", function() {
    const container = document.getElementById('animation-container3');
    
    // Load the Lottie animation
    lottie.loadAnimation({
        container: container, // Corrected container ID
        renderer: 'svg', // Render as SVG
        loop: true, // Loop the animation
        autoplay: true, // Start playing automatically
        path: 'animation6.json' // Path to your animation JSON file
    });
});
